header:
  version: 11

repos:
  meta-mender:
    url: https://github.com/mendersoftware/meta-mender.git
    branch: "#-REF_SPEC-#"
    layers:
      meta-mender-core:

local_conf_header:
  mender-artifact: |
    MENDER_ARTIFACT_NAME = "gha_autobuild"
    INHERIT += "mender-full"
